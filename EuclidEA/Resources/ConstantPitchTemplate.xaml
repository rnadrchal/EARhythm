<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:vm="clr-namespace:EuclidEA.ViewModels"
                    xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
                    xmlns:converters="clr-namespace:EuclidEA.Converters">
    <converters:NoteNameConverter x:Key="NoteName" />

    <DataTemplate x:Key="NoteTemplate">
        <TextBlock Text="{Binding Converter={StaticResource NoteName}}" />
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:ConstantPitchGeneratorViewModel}">
        <StackPanel Orientation="Vertical">
            <Grid HorizontalAlignment="Center" VerticalAlignment="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0"
                           Text="Note" Style="{StaticResource LabelStyle}" />
                <syncfusion:SfRadialSlider Grid.Column="0" Grid.Row="0" Width="120" Height="120" Margin="-16,0"
                                           Minimum="0" Maximum="11"
                                           Value="{Binding Note}"
                                           OuterRimRadiusFactor="0.6"
                                           OuterRimStroke="#444"
                                           OuterRimStrokeThickness="3"
                                           InnerRimFill="#666"
                                           InnerRimStroke="#444"
                                           InnerRimRadiusFactor="0.4"
                                           StartAngle="135" EndAngle="405"
                                           TickFrequency="8"
                                           TickVisibility="Visible"
                                           TickRadiusFactor="0.6" PointerRadiusFactor="0.5" 
                                           LabelVisibility="Hidden"
                                           FontFamily="Kristen ITC">
                    <TextBlock Text="{Binding Note, Converter={StaticResource NoteName}}" FontSize="10" />
                </syncfusion:SfRadialSlider>

                <TextBlock Grid.Row="0" Grid.Column="1" Text="Octave" Style="{StaticResource LabelStyle}" />
                <syncfusion:SfRadialSlider Grid.Column="1" Grid.Row="1" Width="120" Height="120" Margin="-16,0"
                                           Minimum="-4" Maximum="4"
                                           Value="{Binding Octave}"
                                           OuterRimRadiusFactor="0.6"
                                           OuterRimStroke="#444"
                                           OuterRimStrokeThickness="3"
                                           InnerRimFill="#666"
                                           InnerRimStroke="#444"
                                           InnerRimRadiusFactor="0.4"
                                           StartAngle="135" EndAngle="405"
                                           TickFrequency="8"
                                           TickVisibility="Visible"
                                           TickRadiusFactor="0.6" PointerRadiusFactor="0.5" 
                                           LabelVisibility="Hidden"
                                           FontFamily="Kristen ITC">
                    <TextBlock Text="{Binding Octave, Mode=OneWay}" FontSize="10" />
                </syncfusion:SfRadialSlider>
            </Grid>
        </StackPanel>
    </DataTemplate>
</ResourceDictionary>