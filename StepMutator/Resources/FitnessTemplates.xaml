<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:evolution="clr-namespace:StepMutator.Models.Evolution"
                    xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
                    xmlns:converters="clr-namespace:Egami.Sequencer.UI.Converters;assembly=Egami.Sequencer.UI">

    <converters:BooleanToOpacityConverter x:Key="BooleanToOpacity" />

    <DataTemplate DataType="{x:Type evolution:NoteFitness}">
        <GroupBox Header="Note Fitness">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <syncfusion:SfRadialSlider Grid.Column="0" Grid.Row="0" Width="100" Height="100" Margin="-12,-18, -12, -12"
                                           Minimum="0" Maximum="1"
                                           Value="{Binding Weight}"
                                           OuterRimRadiusFactor="0.6"
                                           OuterRimStroke="#444"
                                           OuterRimStrokeThickness="3"
                                           InnerRimFill="#666"
                                           InnerRimStroke="#444"
                                           InnerRimRadiusFactor="0.4"
                                           StartAngle="135" EndAngle="405"
                                           TickVisibility="Visible"
                                           TickRadiusFactor="0.6" PointerRadiusFactor="0.5" 
                                           LabelVisibility="Hidden"
                                           FontFamily="Kristen ITC">
                    <TextBlock  FontSize="10">
                        <Run Text="{Binding Weight, Mode=OneWay}" />
                    </TextBlock>
                </syncfusion:SfRadialSlider>
                <TextBlock Grid.Column="0" Grid.Row="1" Text="Weight" HorizontalAlignment="Center" FontSize="10" Margin="0,-5,0,5" />
                <syncfusion:SfRadialSlider Grid.Column="1" Grid.Row="0" Width="100" Height="100" Margin="-12,-18, -12, -12"
                                           Minimum="21" Maximum="108"
                                           Value="{Binding TargetNote}"
                                           OuterRimRadiusFactor="0.6"
                                           OuterRimStroke="#444"
                                           OuterRimStrokeThickness="3"
                                           InnerRimFill="#666"
                                           InnerRimStroke="#444"
                                           InnerRimRadiusFactor="0.4"
                                           StartAngle="135" EndAngle="405"
                                           TickFrequency="16"
                                           TickVisibility="Visible"
                                           TickRadiusFactor="0.6" PointerRadiusFactor="0.5" 
                                           LabelVisibility="Hidden"
                                           FontFamily="Kristen ITC">
                    <TextBlock  FontSize="10">
                        <Run Text="{Binding TargetNote, Mode=OneWay}" />
                    </TextBlock>
                </syncfusion:SfRadialSlider>
                <TextBlock Grid.Column="1" Grid.Row="1" Text="Target Note" HorizontalAlignment="Center" FontSize="10" Margin="0,-5,0,5"></TextBlock>
            </Grid>

        </GroupBox>
    </DataTemplate>

    <DataTemplate DataType="{x:Type evolution:VelocityFitness}">
        <GroupBox Header="Velocity Fitness">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <syncfusion:SfRadialSlider Grid.Row="0" Grid.Column="0" Width="100" Height="100" Margin="-12,-18, -12, -12"
                                           Minimum="0" Maximum="1"
                                           Value="{Binding Weight}"
                                           OuterRimRadiusFactor="0.6"
                                           OuterRimStroke="#444"
                                           OuterRimStrokeThickness="3"
                                           InnerRimFill="#666"
                                           InnerRimStroke="#444"
                                           InnerRimRadiusFactor="0.4"
                                           StartAngle="135" EndAngle="405"
                                           TickVisibility="Visible"
                                           TickRadiusFactor="0.6" PointerRadiusFactor="0.5" 
                                           LabelVisibility="Hidden"
                                           FontFamily="Kristen ITC">
                    <TextBlock  FontSize="10">
                        <Run Text="{Binding Weight, Mode=OneWay}" />
                    </TextBlock>
                </syncfusion:SfRadialSlider>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Weight" HorizontalAlignment="Center" FontSize="10" Margin="0,-5,0,5" />
                <syncfusion:SfRadialSlider Grid.Row="0" Grid.Column="1" Width="100" Height="100" Margin="-12,-18, -12, -12"
                                           Minimum="0" Maximum="127"
                                           Value="{Binding TargetVelocity}"
                                           OuterRimRadiusFactor="0.6"
                                           OuterRimStroke="#444"
                                           OuterRimStrokeThickness="3"
                                           InnerRimFill="#666"
                                           InnerRimStroke="#444"
                                           InnerRimRadiusFactor="0.4"
                                           StartAngle="135" EndAngle="405"
                                           TickFrequency="16"
                                           TickVisibility="Visible"
                                           TickRadiusFactor="0.6" PointerRadiusFactor="0.5" 
                                           LabelVisibility="Hidden"
                                           FontFamily="Kristen ITC">
                    <TextBlock  FontSize="10">
                        <Run Text="{Binding TargetVelocity, Mode=OneWay}" />
                    </TextBlock>
                </syncfusion:SfRadialSlider>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="Target Velocity" HorizontalAlignment="Center" FontSize="10" Margin="0,-5,0,5"></TextBlock>
            </Grid>

        </GroupBox>
    </DataTemplate>

    <DataTemplate DataType="{x:Type evolution:OffFitness}">
        <GroupBox Header="Off Fitness">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <syncfusion:SfRadialSlider Grid.Row="0" Grid.Column="0" Width="100" Height="100" Margin="-12,-18, -12, -12"
                                           Minimum="0" Maximum="1"
                                           Value="{Binding Weight}"
                                           OuterRimRadiusFactor="0.6"
                                           OuterRimStroke="#444"
                                           OuterRimStrokeThickness="3"
                                           InnerRimFill="#666"
                                           InnerRimStroke="#444"
                                           InnerRimRadiusFactor="0.4"
                                           StartAngle="135" EndAngle="405"
                                           TickVisibility="Visible"
                                           TickRadiusFactor="0.6" PointerRadiusFactor="0.5" 
                                           LabelVisibility="Hidden"
                                           FontFamily="Kristen ITC">
                    <TextBlock  FontSize="10">
                        <Run Text="{Binding Weight, Mode=OneWay}" />
                    </TextBlock>
                </syncfusion:SfRadialSlider>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Weight" HorizontalAlignment="Center"  FontSize="10" Margin="0,-5,0,10" />
                <Button Grid.Row="0" Grid.Column="1" Style="{StaticResource Round3DButton}" Height="50" Width="50"
                        Command="{Binding ToggleOnOffCommand}">
                    <Ellipse Style="{StaticResource LedEllipseStyle}" Width="14" Height="14"
                             Margin="0,10" Opacity="{Binding FavorOn, Converter={StaticResource BooleanToOpacity}}" />
                </Button>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="Prefer ON" HorizontalAlignment="Center" FontSize="10" Margin="0,5"></TextBlock>
            </Grid>
        </GroupBox>
    </DataTemplate>

    <DataTemplate DataType="{x:Type evolution:TieFitness}">
        <GroupBox Header="Legato Fitness">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <syncfusion:SfRadialSlider Width="100" Height="100" Margin="-12,-18, -12, -12"
                                           Grid.Row="0" Grid.Column="0"
                                           Minimum="0" Maximum="1"
                                           Value="{Binding Weight}"
                                           OuterRimRadiusFactor="0.6"
                                           OuterRimStroke="#444"
                                           OuterRimStrokeThickness="3"
                                           InnerRimFill="#666"
                                           InnerRimStroke="#444"
                                           InnerRimRadiusFactor="0.4"
                                           StartAngle="135" EndAngle="405"
                                           TickVisibility="Visible"
                                           TickRadiusFactor="0.6" PointerRadiusFactor="0.5" 
                                           LabelVisibility="Hidden"
                                           FontFamily="Kristen ITC">
                    <TextBlock  FontSize="10">
                        <Run Text="{Binding Weight, Mode=OneWay}" />
                    </TextBlock>
                </syncfusion:SfRadialSlider>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Weight" HorizontalAlignment="Center"  FontSize="10" Margin="0,-5,0,10" />
                <Button Grid.Row="0" Grid.Column="1" Style="{StaticResource Round3DButton}" Height="50" Width="50"
                        Command="{Binding ToggleTieCommand}">
                    <Ellipse Style="{StaticResource LedEllipseStyle}" Width="14" Height="14"
                             Margin="0,10" Opacity="{Binding Legato, Converter={StaticResource BooleanToOpacity}}" />
                </Button>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="Legato ON" HorizontalAlignment="Center" FontSize="10" Margin="0,5"></TextBlock>
            </Grid>
        </GroupBox>
    </DataTemplate>

    <DataTemplate DataType="{x:Type evolution:PitchbendFitness}">
        <GroupBox Header="Pitchbend Fitness">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <syncfusion:SfRadialSlider Grid.Row="0" Grid.Column="0" Width="100" Height="100" Margin="-12,-18, -12, -12"
                                           Minimum="0" Maximum="1"
                                           Value="{Binding Weight}"
                                           OuterRimRadiusFactor="0.6"
                                           OuterRimStroke="#444"
                                           OuterRimStrokeThickness="3"
                                           InnerRimFill="#666"
                                           InnerRimStroke="#444"
                                           InnerRimRadiusFactor="0.4"
                                           StartAngle="135" EndAngle="405"
                                           TickVisibility="Visible"
                                           TickRadiusFactor="0.6" PointerRadiusFactor="0.5" 
                                           LabelVisibility="Hidden"
                                           FontFamily="Kristen ITC">
                    <TextBlock  FontSize="10">
                        <Run Text="{Binding Weight, Mode=OneWay}" />
                    </TextBlock>
                </syncfusion:SfRadialSlider>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Weight" HorizontalAlignment="Center" FontSize="10" Margin="0,-5,0,5" />
                <syncfusion:SfRadialSlider Grid.Row="0" Grid.Column="1" Width="100" Height="100" Margin="-12,-18, -12, -12"
                                           Minimum="-50" Maximum="50"
                                           Value="{Binding Cents}"
                                           OuterRimRadiusFactor="0.6"
                                           OuterRimStroke="#444"
                                           OuterRimStrokeThickness="3"
                                           InnerRimFill="#666"
                                           InnerRimStroke="#444"
                                           InnerRimRadiusFactor="0.4"
                                           StartAngle="135" EndAngle="405"
                                           TickFrequency="16"
                                           TickVisibility="Visible"
                                           TickRadiusFactor="0.6" PointerRadiusFactor="0.5" 
                                           LabelVisibility="Hidden"
                                           FontFamily="Kristen ITC">
                    <TextBlock  FontSize="10">
                        <Run Text="{Binding Cents, StringFormat=N0, Mode=OneWay}" />
                    </TextBlock>
                </syncfusion:SfRadialSlider>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="Target Cents" HorizontalAlignment="Center" FontSize="10" Margin="0,-5,0,5"></TextBlock>
            </Grid>

        </GroupBox>
    </DataTemplate>

    <DataTemplate DataType="{x:Type evolution:ControlChangeFitness}">
        <GroupBox Header="CC Fitness">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <syncfusion:SfRadialSlider Grid.Row="0" Grid.Column="0" Width="100" Height="100" Margin="-12,-18, -12, -12"
                                           Minimum="0" Maximum="1"
                                           Value="{Binding Weight}"
                                           OuterRimRadiusFactor="0.6"
                                           OuterRimStroke="#444"
                                           OuterRimStrokeThickness="3"
                                           InnerRimFill="#666"
                                           InnerRimStroke="#444"
                                           InnerRimRadiusFactor="0.4"
                                           StartAngle="135" EndAngle="405"
                                           TickVisibility="Visible"
                                           TickRadiusFactor="0.6" PointerRadiusFactor="0.5" 
                                           LabelVisibility="Hidden"
                                           FontFamily="Kristen ITC">
                    <TextBlock  FontSize="10">
                        <Run Text="{Binding Weight, Mode=OneWay}" />
                    </TextBlock>
                </syncfusion:SfRadialSlider>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Weight" HorizontalAlignment="Center" FontSize="10" Margin="0,-5,0,5" />
                <syncfusion:SfRadialSlider Grid.Row="0" Grid.Column="1" Width="100" Height="100" Margin="-12,-18, -12, -12"
                                           Minimum="0" Maximum="127"
                                           Value="{Binding TargetValue}"
                                           OuterRimRadiusFactor="0.6"
                                           OuterRimStroke="#444"
                                           OuterRimStrokeThickness="3"
                                           InnerRimFill="#666"
                                           InnerRimStroke="#444"
                                           InnerRimRadiusFactor="0.4"
                                           StartAngle="135" EndAngle="405"
                                           TickFrequency="16"
                                           TickVisibility="Visible"
                                           TickRadiusFactor="0.6" PointerRadiusFactor="0.5" 
                                           LabelVisibility="Hidden"
                                           FontFamily="Kristen ITC">
                    <TextBlock  FontSize="10">
                        <Run Text="{Binding TargetValue, Mode=OneWay}" />
                    </TextBlock>
                </syncfusion:SfRadialSlider>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="Target Value" HorizontalAlignment="Center" FontSize="10" Margin="0,-5,0,5" />
            </Grid>

        </GroupBox>
    </DataTemplate>


</ResourceDictionary>